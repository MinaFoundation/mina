---
name: 1 - Pull Requests

on:
  pull_request:
  push:
    branches:
      - 28-analyze-dhall-files

concurrency:
  group: '${{ github.workflow }} @ ${{ github.head_ref || github.ref }}'
  cancel-in-progress: true

jobs:
  lint-OCaml-check:
    uses: ./.github/workflows/lint-OCaml-check.yaml
